# 지도 기반 삽도 엔진 (종합 도시환경 공간 분석 툴)

이승원 대표(Andy)의 프로젝트다. 존댓말. 팩트 기반, 추정 금지(불확실은 '확인 필요'). 환각 금지. 리스크는 직설적으로 지적한다.

## 세션 시작 시 먼저 읽을 것

본부(HQ) = `~/Library/CloudStorage/GoogleDrive-*/내 드라이브/hayday-hq`

| 파일 | 내용 |
|---|---|
| `03_시스템_도구/지도삽도엔진/00_개요.md` | 한 줄 정의, 원자료 위치표, 관계자 |
| `03_시스템_도구/지도삽도엔진/01_고정지식*.md` | 규격·상수·API. **4분할 + 기술부록 10분할 = 14개** |
| `03_시스템_도구/지도삽도엔진/02_의사결정.md` | 결정과 WHY, 폐기안 |
| `03_시스템_도구/지도삽도엔진/04_미결.md` | 막힌 것, 대표 결정 대기 |
| `.claude/지도삽도엔진_SOP.md` | 반복 실행 절차 (데이터 재수집 I-2/I-3 포함) |

## 이 저장소의 현재 상태 — 먼저 알아야 할 것

- 원격: `github.com/andy555-cmyk/gijang-map` (public, main). 라이브: `https://web-gijang-map-mrksc1tcf2e7efab.sel3.cloudtype.app`
- 결과물은 MapLibre GL JS 기반 **자기완결형 단일 HTML**이다.
  - `index.html` (5KB, 랜딩) / `gijang.html` (2.2MB, 기장 3,430동) / `seogu.html` (5.4MB, 서구 21,146동)
- ⚠ **빌드 파이프라인이 저장소에 없다.** `build.py`·`gen.py`·`pull_hub.sh`·`out_*.json`은 이전 세션의 임시 컨테이너(`/home/claude/seogu/`)에서만 존재했고 소멸했다.
  → **지금은 HTML을 직접 편집하는 것 외에 방법이 없고, 재생성도 불가능하다.**
  → 최우선 과제는 기능 추가가 아니라 **빌드 스크립트 복구 후 저장소에 커밋**하는 것이다. 같은 손실을 반복하지 않는다.
- ⚠ **형제 서비스 `soetae-analysis`(쇠퇴 프로그램)는 별개다. 건드리지 말 것.**

## 방향 (범위 재정의됨)

기장 만화천은 **시작 지역일 뿐**이다. 특정 지역 대시보드가 아니라 **어느 지역이든 구역만 그리면 판정이 나오는 범용 엔진**이 목표다. 현재 1순위는 부산 서구(남부민2동 8·9구역) 버전이며 11월 공고 대비 선작업이다.

## 규칙

- 인물 호칭은 **남실장님**으로 고정. 이름 기반 표기 금지.
- API 키·토큰 값은 코드·문서 어디에도 기재하지 않는다. 실값 출처는 노션 API 키 트래커뿐이다.
- 대용량 HTML 편집 시 먼저 백업 커밋을 만든다. 5.4MB 파일은 한 번 깨지면 복구 수단이 없다.
- 작업이 끝나면 본부 `03_시스템_도구/지도삽도엔진/03_진행기록.md`에 날짜순으로 경과를 남긴다.
- 새로 막힌 것은 같은 폴더 `04_미결.md`에 적는다.

## 통합관리 규약

이 세션은 **이 저장소만** 본다. 통합관리는 본부(HQ) SIGMA 세션이 한다.
보드: `03_시스템_도구/00_개발통합관리.md` — **세션 시작 시 읽는다.**

- 작업 시작: `git pull --rebase`
- 작업 종료: **반드시 커밋·푸시.** 미커밋 상태로 세션을 끝내지 않는다.
- 종료 시 본부 `03_진행기록.md` 갱신 + 새 미결은 `04_미결.md`. 빠뜨리면 통합관리자가 이 작업을 못 본다.
- 프로젝트를 가로지르는 판단(우선순위·가격·상품화·다른 건과의 충돌)은 **여기서 결정하지 않는다.** 보드 §3에 올리고 본부에 넘긴다.
- 다른 저장소(`narajangteo`, `soetae-analysis`)를 건드리지 않는다.

### 이 저장소에 걸린 교차 병목 (본부 보드 §3)

- ✅ GitHub 인증 해소(2026-07-28). SSH 키 `~/.ssh/id_ed25519_hayday`로 3개 저장소 push 가능. 저장소별 `core.sshCommand` 고정됨
- **서구청 건축과 051-240-4612** — 빈집 지번 확보가 모든 후속 작업의 선행. 정보공개법 제7조 비치정보면 청구 20일을 건너뛴다

**2026-07-28 대표 지시로 교차 병목에서 제외된 것 — 다시 올리지 않는다.**
- 노후 건축물 판정 연수(16년/20년/30년) → 현 엔진값 유지
- 남실장님 "현황분석 기법 교육 세션" 날짜 대기
